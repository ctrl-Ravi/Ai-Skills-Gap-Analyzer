# Deployment API URL Configuration
# For local development, this relies on the default fallback (http://127.0.0.1:8000)
# For production (e.g. Vercel), set this in your dashboard environment variables
VITE_API_URL=https://my-backend-app.onrender.com

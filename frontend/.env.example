# Deployment API URL Configuration
# For local development, this file is not needed as the app gracefully falls back to (http://127.0.0.1:8000)
# For production (e.g. Vercel), set the VITE_API_URL in your hosting provider's dashboard
VITE_API_URL=https://my-backend-app.onrender.com
